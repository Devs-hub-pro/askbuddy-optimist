<view class="state-card card">
  <view class="title">{{title}}</view>
  <view class="subtitle">{{desc}}</view>
  <button wx:if="{{actionText}}" class="button-primary action-btn" bindtap="onTapAction">{{actionText}}</button>
</view>
