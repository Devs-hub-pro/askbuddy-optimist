import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.lovable.bf11243b7233439588451aa876a26efd',
  appName: 'askbuddy-optimist',
  webDir: 'dist',
  ios: {
    path: 'apps/ios'
  },
  android: {
    path: 'apps/android'
  }
};

export default config;
