# 微信小程序最小验收报告（Step 5）

## 验收范围
- 基础框架与路由注册
- 核心主链路页面可打开
- Mock 数据可驱动列表、详情、提问提交
- 下拉刷新/分享函数声明
- 授权流程（代码路径）

## 已完成（本地可验证）
1. `app.json` 路由与 tabBar 注册完成。
2. 7 个核心页面、1 个全局状态组件、3 个工具模块已落地。
3. 全量 JSON 解析通过（`json ok`）。
4. 全量 JS 语法检查通过（`js syntax ok`）。
5. 提问 -> 详情跳转链路可用（Mock）。

## 未完成（需微信开发者工具/真机）
1. 真机授权弹窗文案与拒绝后回退交互。
2. 分享卡片在会话/群聊中的展示样式。
3. iPhone 刘海屏/底部 Home Indicator 的安全区实测。
4. 下拉刷新体验（阻尼、完成态）是否与 iOS 感知接近。
5. Tab 页切换动效与页面栈返回行为的真机一致性。

## 问题清单（当前阻塞/风险）
1. 当前为 Mock-only，`utils/request.js` 未启用真实 RPC，后端联调前所有数据非真实源。
2. `project.config.json` 使用 `touristappid`，仅可本地调试，发布前需替换正式 appid。
3. 详情页目前只覆盖问题详情，尚未扩展专家/话题详情。

## 下一步建议
1. 在微信开发者工具导入 `apps/wechat-miniprogram`，先跑完 P0 手工回归。
2. 按 A 口径补齐真实 RPC 映射（仅替换 `utils/request.js`）。
3. 逐页从 Mock 切换真实接口，保留降级回退开关。
